#include <iostream>
#include <string>
using namespace std;

bool isPalindrome(string stringOne);

int main(){

    string s1 = "GeeksforGeeks";
    std::cout << "Is Palindrome?: " << isPalindrome(s1);
}

bool isPalindrome(string stringOne){
    
    string reversed = stringOne;
    reverse()
    std::for_each(nums.cbegin(), nums.cend(), print);

}